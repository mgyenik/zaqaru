[package]
name = "interp-spike"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
iced-x86 = { version = "1.21.0", features = ["code_asm"] }

[profile.release]
opt-level = 3
lto = true
